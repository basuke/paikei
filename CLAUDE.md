# Paikei — 麻雀局面解析エンジン

**Paikei（牌景）** はプロジェクト・iOSアプリ・テキストフォーマットに共通する名前。
共有ライブラリは **PaikeiCore**。プロジェクトの紹介と使い方は `README.md`、
フォーマットの正典は `docs/PAIKEI_SPEC.md`。

## このプロジェクトは何か

**最終的な野望**: iOSアプリ **Paikei**。カメラで雀卓を撮影すると牌を認識し、
その瞬間の局面についてあらゆる解析を返す。

このリポジトリはその**ロジック層**。カメラ認識（Vision / Core ML）は将来の別レイヤーで、
両者はテキストフォーマット **Paikei** で疎結合にする。認識層の完成を待たずに、
ロジック層を単体で完成・検証できるようにするのが狙い。

```
[カメラ認識層]──┐
[MJAI牌譜]────┼──> .paikei ──> [PaikeiCore] ──> 解析結果
[手入力/REPL]──┘                (このリポジトリ)
```

## 言語と環境

- **Swift 6**（strict concurrency有効）、SwiftPMパッケージ
- **コアはLinuxでもビルド・テスト可能にする**こと。開発はmacOSとWSL2 Ubuntu
  （swiftly導入済み）の両方で行う。Foundationへの依存は最小限に、
  UIKit/AppKit/CoreML等のApple専用フレームワークはコアに持ち込まない
- 依存は原則 `swift-argument-parser`（CLI用）のみ。テストは **swift-testing**

## パッケージ構成

新規ファイルの置き場所（未作成のディレクトリは必要になったら作る）:

- `Sources/PaikeiCore/`
  - `Model/` … 牌・風・プレイヤー・副露・河。局にも対局にも属さない素材
  - `Game/` … 1局（`GameState` / `PlayerState` / `Phase` / `Event` / `GameTimeline` /
    `GameResult`）。配牌から和了・流局まで
  - `Shanten/` … シャンテン数・受け入れ・何切る
  - `Agari/` … 面子分解・役・平和・和了形の読み
  - `Scoring/` … 符・点数・ドラ・応答の選択肢
  - `Safety/` … 論理捨て牌履歴・フリテン・安牌・見えている牌
  - `Format/` … `.paikei` の入出力。`Format/Notation/` は値の表記（仕様§2のMPSZ・
    §4の副露・§5の河）、`Format/Stream/` は仕様§8とMJAI方言
  - `Bot/` … 局面から手を決める打ち手
  - `Match/` … 局の連鎖（`Match` / `MatchState`）。半荘・東風戦
  - `Rules/` … `RuleSet`。層をまたぐ卓の取り決めなのでどの層にも属さない
- `Sources/paikei/` … CLI実行ファイル（PaikeiCoreに依存）
- `Tests/PaikeiCoreTests/Fixtures/` … 仕様§9のサンプルとシャンテン正解データ

設計原則:

- **ライブラリ内の依存は一方向**: Rules/Model/Game/Shanten/Agari/Scoring/Safety/Bot/Match
  （ドメイン層）は Format/ の型を一切参照しない。フォーマットは入出力の1形式にすぎず、
  依存は常に Format → ドメイン層 の向きのみ
- **ドメイン層の中も三層**: `Model`（素材）→ `Game`（局）→ `Match`（対局）。
  「局の中身」か「局と局のあいだ」かが `Game` と `Match` の境目で、
  受け渡しは `GameResult` 1つに絞る（点数の移動は `Match` の担当）
- **ドメイン型は自分の表記を持たない**。`Tile` の MPSZ も `Meld` の `pon(5'55p,L)` も
  `RiverTile` の `4m+*` も、構文は `Format/Notation/` の担当で型の側には置かない。
  ロジックが表記に依存すると、フォーマットを変えたときに答えが変わってしまう
  （高点法の同点決着が MPSZ 文字列比較だった実績がある）。
  牌の表記が3つある（MPSZ / Unicode / MJAI）ことからも、どれも `Tile` の一部ではなく
  変換先だと分かる。唯一の例外は `rawValue` が仕様の表記トークンである enum
  （`case 東 = "E"` `case 萬子 = "m"`）— 1トークンの対応表は型に持たせてよい
- 全て値型（struct/enum）。ロジックは純粋関数にしてテスト容易性を最優先
- 「不明」は第一級の概念（仕様§1・§6）。解析関数は不足情報があるとき
  「仮定を明示して答える」か「必要な情報を宣言して断る」— 黙って推測しない。
  仮定として置いてよいのは「外れていれば答えがより高くなる」ものだけ
- **矛盾はコアが検出して返す**。ライブラリなのだから、おかしな入力を黙って握りつぶさず
  何がおかしいかを型で返す（`EventApplicationError` / `WinContextError` / `Requirement`）
- `GameState` は「いまの卓上」だけを表し履歴を持たない（カメラ由来には履歴が無いため）。
  履歴が要る導出は `GameTimeline`（初期局面 + イベント列）の担当
- イベント適用は `apply(GameState, Event) throws -> GameState` の形に統一。
  REPLの遷移コマンド、ストリーム再生、MJAI botモードすべてがこの関数を共有する

## 命名規約

Swift の Unicode 識別子を活かし、麻雀の概念はそのまま日本語で書く。

- **enum の case は日本語**。ラベルは、**牌や属性は日本語、プレイヤーの役割は
  英語の前置詞**にする。関数側（`可能な応答(for:)` `通った牌(against:)`）と揃えるため
  `case ポン(of: Player, from: Player, 牌: Tile, 手牌から: [Tile])`
  （enum の case は関数と違い、外部ラベルと内部名を分けられない。
  ラベルを消したいときは `_` を使う）
- **`Sources/` の変数名は英数字**。パターンバインドも英数字にする
  （`case let .ポン(actor, target, tile, consumed)`）。
  ラベルは日本語でも、それを受ける側はプログラミングの文脈なので英数字が読みやすい。
  **テストのローカル変数は日本語も可** — `let 履歴あり` / `let 履歴なし` のように
  対比がそのままテストの筋書きになる場合は日本語の方が読みやすい
- **Bool を返す述語は日本語で「〜か」**。プロパティも関数も同じ。
  `tile.字牌か` `tile.么九牌か` `hand.門前か` `match.終局済みか` /
  `オーラスか(rules:)` `嶺上ツモか(of:)`。
  Swift でプロパティと引数なし関数を分けるのは実装上の都合でしかないので、
  そこで言語を変えない。格納か計算かも呼ぶ側からは見えないので区別しない
  （`Tile.赤か` は格納プロパティ）。
- **プロパティも麻雀用語なら日本語**。`GameState.場風` `.局` `.本場` `.供託`、
  `PlayerState.席風` `.立直`、`Score.翻` `.符` `.ドラ` `.本場` `.供託`、
  `DoraCount.表` `.赤` `.裏`、`Ukeire.シャンテン`、`RuleSet.喰いタン` `.一発`。
  汎用語は英語のまま（`suit` `rank` `tiles` `players` `payment` `limit` `total`）
- **同じ綴りが4役を兼ねる**ので、プロパティを日本語にしても以下は据え置く:
  `.paikei` のキー（`case "bakaze":`）、CLI のフラグ（`@Option var bakaze` →
  `--bakaze`）、REPL のトークン（`case "ippatsu":`）、ローカル変数
  （`if let bakaze = 場風`）
- **日本語ラベルの引数には英数字の内部名を付ける**。`結末(テンパイ tenpai:)`
  `payment(翻 han:符 fu:)`。ラベルは日本語でも、本体で受ける名前は変数だから
- **型名は英語で素直に書けるならそのまま**。`Player` `GameState` `Tile` は英語のまま
- **ローマ字は英語ではない**。`shimocha` / `toimen` / `kamicha` のように日本語をラテン
  文字で綴っただけの名前は日本語にする（`Player` の case は 自分/下家/対面/上家）。
  「英語で素直に書けている」の判定を誤りやすいのがここ。
  予約語を避けるための綴り（`myself`）も、日本語にすれば問題ごと消える
- **関数名は麻雀の概念なら日本語**。`可能な応答(for:)` `通った牌(against:)`。
  汎用の操作（`applying` `serialized`）は英語
- **エラーの case は体言止めで短く**。`case 山切れ` `case 自席未確定` `case ポンなしの加槓(Player, Tile)`
- **rawValue が仕様の表記トークンなら ASCII 固定**。`case 東 = "E"` `case チー = "chi"`
- **テストの型名・関数名も日本語**。`@Suite("応答の選択肢") struct 応答の選択肢`、
  `@Test func 立直中は鳴けない()`。
  `swift test --filter` は日本語の識別子で効くが `@Test("表示名")` では効かない

リネーム時の注意:

- **sed でリネームしない**。`bakaze` のようにプロパティ名がフォーマットのキーや
  CLI フラグと同じ綴りだと、文字列リテラルまで書き換わる。しかも
  パーサー・シリアライザ・フィクスチャが同時に変わると**テストが緑のまま
  互換性が壊れる**（実際にやった）。逆向き（日本語→英数字）はさらに危険で、
  enum の case まで巻き込んでコンパイルは通るのに規約違反が埋まる
- **`LSP` の `findReferences` で参照位置を取り、その位置だけ置換する**。同名の
  ローカル変数を巻き込まない。注意点2つ:
  - `character` は **UTF-8 バイトオフセット**（UTF-16 でも文字数でもない）
  - `#expect` マクロ内の参照はマクロ開始位置で返る。位置が一致しないものは
    適用せず報告し、同じ行の実位置（別途返る）に任せる
  - メンバワイズ init の引数ラベル（`Score(翻:符:…)`）は拾わないので別途直す
- **1シンボルずつ**。適用したら行の長さが変わって以降の位置がずれるので、
  シンボルごとに `findReferences` を取り直す。各シンボルでビルド＆テストして
  コミットする
- **`swift test` の終了コードをパイプで潰さない**。`swift test | head -3` は
  常に成功するので、壊れたままコミットした実績がある。
  `swift test > log 2>&1; echo "exit=$?"` の形で確認する
- 置換後は `grep -rnE "(let|var) [^ -~][^ ]* *=|if let [^ -~]|guard let [^ -~]" Sources`
  でローカル変数に日本語が混ざっていないか確認する（プロパティ宣言は
  `let 役: Set<Yaku>` のように初期化子が無いので引っかからない）

## CLI: `paikei`

1発実行・REPL・MJAI botの3つの顔を持つ。使用例は `README.md`。

```
$ paikei analyze <path>            # 局面の要約
$ paikei shanten <path>            # シャンテン・受け入れ・何切る
$ paikei score <path> <牌> <tsumo|ron> [フラグ]
$ paikei safety <path> [相手]      # 現物/スジ/無スジ
$ paikei replay <path>             # [stream] の再生
$ paikei mjai                      # MJAI botとして標準入出力で対局
$ paikei                           # REPL起動

PAIKEI_TILES=unicode               # 牌の表示を Unicode 麻雀牌に（既定は漢字）
```

REPLのコマンドは局面フェーズ（仕様§7）で有効性をチェックする
（例: `score X ron` は応答待ちでのみ有効）。遷移コマンド（`discard` `pon` `chi` …）は
イベントを生成するので、セッションがそのまま `.paikei` として保存できる（仕様§8.4）。

## 実装フェーズと現在地

1〜7 は完了。フェーズを1つずつ、テストが通ってから次へ進める方針は継続する。

| # | フェーズ | 状態 |
|---|---|---|
| 1 | Tile + MPSZ | 完了 |
| 2 | Snapshotパーサー + Phase導出 | 完了 |
| 3 | シャンテン + 受け入れ | 完了（正解データ4万手と照合） |
| 4 | 和了・役・符・点数 | 完了（高点法・役満複合まで） |
| 5 | フリテン + 安牌 | 完了（現物・スジ・壁） |
| 6 | ストリーム（イベント・mjai方言・REPL） | 完了 |
| 7 | MJAI botモード | 完了（`paikei mjai` + `SimpleBot`） |

### 既知の未実装（意識して残しているもの）

- **裁定を持たない**。応答の優先順位（頭ハネ）、途中流局の成立、テンパイ判定は
  卓が決めること。`Match/` は結果（`GameResult`）を受けて点数移動と局の繋ぎ方だけを担う
- **西入とウマ・オカが無い**。規定局数を終えたら終局し、`Match.standings` は
  持ち点の順位までを返す
- **`Event` は局の中だけ**。和了イベントは応答対象を消費するだけで持ち点を動かさない。
  持ち点の移動は `MatchState.applying(_:at:rules:)` の担当
- **暗槓の槍槓だけは導出しない**。国士無双限定というルール依存があるため、
  呼び出し側が `WinOptions.robbingKan` で指定する。
  一発・嶺上開花（履歴）と海底/河底・加槓の槍槓・天和/地和（局面）は導出済み
- **途中流局の成立を判定しない**。四開槓・四風連打・九種九牌は「卓が流局と裁定した
  結果」を `RyukyokuReason` として受け取るだけ。頭ハネの優先順位と同じで裁定は卓の側
- **`SimpleBot` は押し引きをしない**。鳴かず立直せず、安全度も点数期待値も見ない

## テスト方針

- シャンテン計算は既知の正解データセット（公開されている手牌×シャンテン数の
  網羅データ）との突き合わせで検証する
- 点数計算は「30符4翻=7700（子ロン）」のような古典的な組を網羅テストに。
  符計算のエッジケース（平和ツモ20符、七対子25符、喰い平和形30符、切り上げ）を重点的に
- パーサーは常にラウンドトリップ（parse → serialize → parse 同一）を検証
- **テスト用の手牌は必ず枚数を数えてから書く**。13枚/14枚のつもりで12枚や15枚を
  書く事故が繰り返し起きている
- 実装の正しさに迷ったら仕様書が正。仕様書に穴を見つけたら
  実装を進める前に仕様書の§10（未解決の論点）へ追記して報告すること

## スコープ外（今はやらない）

- カメラ・画像認識（別レイヤー。ただしコアの設計判断で認識層を意識するのは歓迎）
- GUI / iOSアプリ本体
- 三人麻雀（四麻のみ）
- 統計的な危険牌推定（第一段階はルールベースの現物・スジ・壁まで）
- **Mortal（AGPL）のコードを読む・移植する・リンクすることは避ける**。
  アルゴリズムの参考はアイデアレベルに留め、ライセンス汚染を防ぐ

## ルールオプションの既定値

喰いタンあり・赤3枚（0m/0p/0s各1）・切り上げ満貫なし・一発/裏あり・
包（大三元/大四喜）あり・大明槓の責任払いなし・人和なし・流し満貫あり（流局扱い）。
対局は半荘戦・25000点持ち・トビ終了・アガリやめなし。

**`RuleSet` に集約し、ハードコードしない**（仕様§10の論点3）。1局のルール
（役・符・点数）と対局のルール（局の繋ぎ方）を1つの型に入れてある —— 雀荘の卓に
貼ってある取り決めが1枚なのと同じで、`rule:` プリセットも1つ引けば済む。
層をまたぐので `Rules/` に置き、評価器にも `Match` にも注入する
（`YakuDetector(rules:)` `FuCalculator(rules:)` `Match(rules:)`）。
